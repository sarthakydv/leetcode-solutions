# Add the following to your user setting.json in VS Code
    "leetcode.filePath": {
        "default": {
            "folder": "src/solutions",
            "filename": "${id}.${kebab-case-name}.${ext}"
        }
    }
